# 三类「重复」分桶清单（只读）

日期：2026-07-21  
报告 JSON：`backups/duplicate_types_inventory.json`  
**未** DELETE / **未** fanout。

## 总表

| 类型 | 是什么 | 规模（活动行口径） | 默认怎么办 |
|---|---|---|---|
| **① 跨群同文** | 同 QQ + 同文（前 200 字）出现在 ≥2 个数字群 | **17,793 族**；若各族只留 1 行约 **112,821** 多余行 | 读侧 **collapse**；物理删需授权 |
| **② 双 Bot 同群** | 同群 + 同人 + 同文，yushu 与 baizz 各一行 | **17,796 族**；约 **19,556** 可视为「第二 bot 多出来的行」；近 24h 仍有少量成对写入 | **通常保留**（双人格）；写侧去重需产品决策 |
| **③ 同人多群真发言** | 同一 QQ 在 ≥2 个群都有发言（内容可不同） | **374 人**，合计约 **13.5 万** 活动行 | **不删**；检索用 QQ；`person scope=all_groups` |

## 样例（①）

- `@羽书(...)` 一类短句 / 「你又卡了吗？」在 5–6 群各有多份  

## 样例（②）

- 150727649 上同句 yushu+baizz 成对（图片、@ 等）  

## 和 blocked 任务关系

protected「禁 destructive / fanout」针对的是：**不要擅自做 ① 的物理删除，也不要再开 fanout 制造 ①**。  
②③ 不是这条锁要自动清掉的对象。
